<div class="container">
<h1>Bienvenue ! </h1>
<br>
<br>
<h2 class="display-4-selection text-center mb-5"> Découvrez notre sélection de chatons (<%= @items.size %>)</h2>

<% @items.each |item| do %>
  <%= item.image_url %>
  <%= links_to <%= item.title %>, item_path(item.id) %>
<% end %>

</div>
